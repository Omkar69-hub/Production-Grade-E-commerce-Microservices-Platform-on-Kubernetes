# Parent POM

Centralized Maven POM for all microservices in the E-Commerce Platform.

## Features
- Standardizes Spring Boot and Spring Cloud versions.
- Centralizes build plugins (Compiler, Surefire).
- Configures Lombok and MapStruct annotation processors globally.
- Enforces consistent dependency versions across the entire stack.
