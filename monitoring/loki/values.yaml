loki:
  auth_enabled: false
  commonConfig:
    replication_factor: 1
  storage:
    type: filesystem
  compactor:
    retention_enabled: true
  limits_config:
    retention_period: 14d

singleBinary:
  replicas: 1
  persistence:
    enabled: true
    size: 20Gi
    storageClass: standard # Update based on environment

test:
  enabled: false
